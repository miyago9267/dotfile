## Phase 1: 架構設計與基礎建設

> Status: completed

設計兩層分離架構，建立 `.ai/` 目錄結構和 `.gitignore` 規則。

- [x] 設計兩層分離架構（規格層 vs 工作記憶層）
- [x] 撰寫 SPEC.md（需求 R1-R10、ADR、Rabbit Holes）
- [x] 建立 `.ai/` 目錄結構（CURRENT.md, HANDOFF.md, changelog.md, lessons.md, sessions/, snapshots/）
- [x] 設定 `.gitignore` 排除 `.ai/`

## Phase 2: 核心 Scripts 實作

> Status: completed

實作所有核心 shell scripts，覆蓋 R1-R10 全部需求。

- [x] `bootstrap.sh` -- 新 session 自舉，讀取 HANDOFF/changelog/lessons（R5）
- [x] `end-session.sh` -- 收工交接，CURRENT -> HANDOFF + session summary（R4）
- [x] `log.sh` -- 追加 changelog 到 `.ai/changelog.md`（R6）
- [x] `lesson.sh` -- 追加經驗教訓到 `.ai/lessons.md`，key 去重（R7）
- [x] `ai-export.sh` -- 匯出 `.ai/` 精選到 `docs/ai/`（R8）
- [x] `spec-archive.sh` -- 封存 tasks/phase 到 `archive/`（R1, R2, R9, R10）
- [x] `snapshot.sh` -- 中段存檔 save/restore/list
- [x] `check.sh` -- 健康檢查，`--init` 初始化 `.ai/` 結構（R3 輔助）
- [x] `skill-create.sh` -- 建立新 skill

## Phase 3: 模板與規則整合

> Status: completed

建立 spec 模板系統，更新 CLAUDE.md 全域行為規則。

- [x] 建立 `docs/specs/_templates/SPEC.template.md`
- [x] 建立 `docs/specs/_templates/TASKS.template.md`
- [x] 建立 `docs/specs/_templates/TESTS.template.md`
- [x] 建立 `docs/specs/_templates/PROGRESS.template.md`
- [x] 更新 CLAUDE.md 反映兩層分離架構
- [x] 定義 Scripts CLI 對照表
- [x] 定義職責分離原則和 Token 節省原則
