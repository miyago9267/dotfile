---
spec: hook-automation-rationalization
batch: 1
created: 2026-05-12
---

# Tests: Claude Hook Automation Rationalization

> Spec: `docs/specs/hook-automation-rationalization/SPEC.md`

## 驗收條件 (EARS)

### R4: When the first batch is implemented, the system shall wire `strategic-compact` into Claude `PreToolUse` for `Edit|Write`

- **When**: Claude 執行 `Edit` 或 `Write`
- **Shall**: 會呼叫 `suggest-compact.sh`
- **驗證方式**: 檢查 `settings.json` 的 `PreToolUse` 設定
- **狀態**: [ ] 未驗證 / [x] 通過

### R5: When the first batch is implemented, the system shall deny `git add .` and `git add -A` style staging via a `PreToolUse/Bash` hook and tell the agent to stage explicit paths

- **When**: hook 收到 `git add .` 或 `git add -A`
- **Shall**: 回傳 deny decision 與明確訊息
- **驗證方式**: 用樣本 JSON pipe 進 hook script
- **狀態**: [ ] 未驗證 / [x] 通過

### R6: When the first batch is implemented, the system shall deny obvious AI attribution commit message patterns via a `PreToolUse/Bash` hook

- **When**: hook 收到含 `Co-Authored-By`、`Generated by` 或 `AI-assisted` 的 `git commit`
- **Shall**: 回傳 deny decision
- **驗證方式**: 用樣本 JSON pipe 進 hook script
- **狀態**: [ ] 未驗證 / [x] 通過

## 測試案例

### 正常路徑

| # | 測試 | 預期結果 | 狀態 |
|---|------|----------|------|
| 1 | 檢查 `settings.json` 有 `Edit|Write` 的 `strategic-compact` hook | 設定存在 | [x] |
| 2 | `git add src/main.ts` | hook 不攔截 | [x] |
| 3 | `git commit -m "fix(api): adjust timeout"` | hook 不攔截 | [x] |

### 邊界案例

| # | 測試 | 預期結果 | 狀態 |
|---|------|----------|------|
| 1 | `git add -A` | deny | [x] |
| 2 | `git add .` | deny | [x] |
| 3 | `git commit -m "docs: update\n\nCo-Authored-By: Claude"` | deny | [x] |

### 錯誤處理

| # | 測試 | 預期結果 | 狀態 |
|---|------|----------|------|
| 1 | hook 收到非 `git` 指令 | 直接通過，不輸出 deny | [x] |
