Please install <PROJECT_NAME> for me.

Fetch `<INSTALL_URL>`/INSTALL.md, read it completely, inspect the referenced
installer before running it, ask for confirmation at the documented boundary,
then follow the playbook and report the selected ref, changed paths, and
verification results. Do not use elevated privileges or bypass the playbook.
