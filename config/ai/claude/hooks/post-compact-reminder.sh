#!/usr/bin/env bash

cat <<'EOF'
After compact, re-anchor on the active spec/progress files, current runtime role, modified files, verification state, and the next pending decision before editing.
EOF
