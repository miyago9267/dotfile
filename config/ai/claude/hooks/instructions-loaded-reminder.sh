#!/usr/bin/env bash

cat <<'EOF'
Runtime routing: deterministic local rules -> hooks; contextual workflows -> skills; live external state -> MCP.
Claude bias: spec, planning, docs, review framing, and small patches before heavy implementation.
EOF
