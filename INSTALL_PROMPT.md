# 安裝 Dotfile

請安裝 Dotfile。

先完整讀取 `https://raw.githubusercontent.com/miyago9267/dotfile/main/INSTALL.md`，
再檢查同一個 ref 的 `install.sh`。依 playbook 回報即將執行的模式與項目，到了
confirmation boundary 就停下來等確認；不要使用 elevated privilege，也不要跳過
playbook。

完成後回報實際使用的 ref、修改的路徑、verification 結果、未驗證的平台，以及
是否留下 backup 或 error log。
