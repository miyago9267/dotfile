# Field Journal

All entries use local paths only; secrets, account data, and raw session content
were excluded.

## 2026-09-15

- Initialized an authorized `offline-sample` case for the owner-operated local
  `grok` executable.
- Confirmed target type and hash with `file` and `shasum`.
- Read the CLI help surface and searched embedded strings for pager module and
  interaction anchors.
- Mapped the observed seams to the standalone `lumen` implementation.
- Did not execute the target for analysis, contact external services, patch the
  target, or copy source/assets from it.
