#!/bin/bash
# 快速 git add + commit + push

if [ -z "$1" ]; then
  echo "Usage: gitquick <commit-message>"
  echo "Example: gitquick 'fix bug'"
  exit 1
fi

git add .
git commit -m "$*"
git push

echo "✅ Done!"
